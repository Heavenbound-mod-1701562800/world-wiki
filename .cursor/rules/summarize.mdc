---
description: Wiki 抓取、引用绑定、按页总结
globs: models/wiki.py,models/dictionary.py,libs/crawler.py,libs/page_store.py,libs/db.py
alwaysApply: false
---

# 总结流水线

`Wiki.run` **按 URL 串行处理**（下载 → 拆章 → 该页章节可走 `llm_queue` 并行）。每页更新 peewee `Page`：`pending → fetching → summarizing → done|partial|failed`。

下载只走 MediaWiki `api.php?action=parse`，HTML 写入 `data/raw/`。本地 `.json` 取 `parse.text` 再包一层文章 HTML。

## Citation（与 Chapter 同级，在 wiki.py）

拆章前 `Citation.bind`：把 `sup.reference` 换成 `〔出处：脚注原文 | 绝对URL〕`，再删 References。不要推断 kind，只用 label。`Chapter.citations` 只挂本章出现过的引用。md 文末 `## 出处` 来自该列表（英文脚注原文 + 链接，不经 LLM）。

## 精简 prompt

入模前 `Dictionary.matches_in` 对本章做大小写不敏感检索（只收已有中文的词，跳过 `source=-1`），命中作为「专名对照」。原文不替换。模型只输出 JSON：`summary`（精简 markdown）和 `untranslated`（对照表未覆盖、须保持英文的专名）；调用带 `response_format={"type": "json_object"}`。解析失败则该章跳过。`summary` 写入 md；`untranslated` 经 `clean_text` 清洗后 `Dictionary.add_local`（`source=3`，`zh` 空）。禁止自行音译。NPC 对话必须写成「据某某所述」。超时见 `LLM_TIMEOUT` 和 `chat()` 重试。

词表优先级 **`1 > 2 > 3 = -1`**：`1` Genshin Dictionary（https://dataset.genshin-dictionary.com/words.json ），`2` Wiki Other Languages，`3` 手动/总结未译，`-1` 不当专名。`sync()` 命中升为 1；`fill_from_wiki` 写成 2（不降级 1）；`add_local` 无同行才写成 3。`lookup_wiki_zh` / `fill_from_wiki` 按组对 `en` 再取 `zhs`。已有 md 需重新 summarize 才会更新。
