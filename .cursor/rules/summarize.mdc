---
description: Wiki 抓取、引用绑定、按页写成英文 Markdown
globs: models/wiki.py,models/dictionary.py,libs/crawler.py,libs/page_store.py,libs/db.py
alwaysApply: false
---

# 抓取流水线

`Wiki.run` **按 URL 串行处理**（下载 → 拆章 → 英文 md 落盘）。每页更新 peewee `Page`：`pending → fetching → summarizing → done|partial|failed`。抓取不再调用聊天模型。

下载只走 MediaWiki `api.php?action=parse`，HTML 写入 `data/raw/`。本地 `.json` 取 `parse.text` 再包一层文章 HTML。

## Citation（与 Chapter 同级，在 wiki.py）

拆章前 `Citation.bind`：把 `sup.reference` 换成 `〔出处：脚注原文 | 绝对URL〕`，再删 References。不要推断 kind，只用 label。`Chapter.citations` 只挂本章出现过的引用。md 文末 `## 出处` 来自该列表（英文脚注原文 + 链接）。

## 英文 md

拆章后每章正文是清洗过的英文原文（已含〔出处〕），写入 `## Content`；首段无 h2 时章节名为 `Introduction`。抽取文本时沿用 HTML 原文空白（不要 `get_text` 夹分隔符），`br` 换成换行，章节兄弟节点之间再换行。图片画廊/缩略图（`.wikia-gallery`、`.lightbox-caption`、`div.thumb` 等）整块丢掉，不把图下注释写进 md。标题大小写不敏感等于 `Other Languages` 的 h2 **不写 md**；从该节 wikitable 抽出 English / Chinese (Simplified)（含多组），`Dictionary.add_from_wiki_pairs` 写成 `source=2`（斜杠别名展开为笛卡尔积；覆盖 3/-1，不降级 1）。

词表优先级 **`1 > 2 > 3 = -1`**：`1` Genshin Dictionary（https://dataset.genshin-dictionary.com/words.json ），`2` Wiki Other Languages，`3` 手动未译，`-1` 不当专名。`sync()` 命中升为 1；`en`/`zh` 的 ` / ` 别名展开为笛卡尔积。`fill_from_wiki` / Admin lookup 仍按词条页 `en` 取 `zhs`。已有 md 需重新抓取才会更新。
